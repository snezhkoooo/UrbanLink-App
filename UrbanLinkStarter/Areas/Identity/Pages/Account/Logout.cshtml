@page
@model LogoutModel
@{
    ViewData["Title"] = "Sign out";
}

<div class="ul-auth-wrap">
    <div class="ul-auth-card ul-enter">
        <h2>Signed out</h2>
        <p style="color: var(--text-mid);">See you around the city.</p>
        <a class="ul-btn ul-btn-secondary" asp-controller="Home" asp-action="Index" style="margin-top: 1rem;">Back home</a>
    </div>
</div>
